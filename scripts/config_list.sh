#!/bin/bash

# Define configs to backup/restore
CONFIGS=(
    "$HOME/.config/sway"
    "$HOME/.config/waybar"
    "$HOME/.config/swaylock"
    "$HOME/.config/kitty"
    "$HOME/.config/alacritty"
    "$HOME/.config/fontconfig"
)
